CodeBlock ("",["ghci"],[]) "> let foo = inj (Identity True) :: OpenSum Identity '[Bool, String]\n\n> prj foo :: Maybe (Identity Int)\n\n<interactive>:4:1: error:\n    \8226 Attempted to call `prj' to produce a `Identity Int'.\n      But the OpenSum can only contain one of:\n        '[Bool, String]\n    \8226 In the expression: prj foo :: Maybe (Identity Int)\n      In an equation for \8216it\8217:\n          it = prj foo :: Maybe (Identity Int)\n\n"